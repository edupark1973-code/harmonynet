# 하모니넷 (Harmonynet) 데이터 모델 정의서 (데이터모델.md)

---

## 1. 개요 (Overview)

본 문서는 **[prd.md](file:///C:/dev/harmonynet/prd.md)**에 명시된 기능 및 비기능 요구사항을 구현하기 위해 필요한 **WP REST API v2** 연동 TypeScript 데이터 구조 및 인터페이스를 정의합니다.

Next.js (App Router) 환경에서 서버 사이드 렌더링(SSR) 및 정적 재생성(ISR) 시 높은 타입 안전성(Type-safety)과 가독성을 보장하도록 작성되었습니다.

---

## 2. 워드프레스 원본 데이터 모델 (WP REST API Raw Models)

워드프레스 기본 REST API(`https://harmonynet.kr/wp-json/wp/v2/`) 응답 특성에 맞춘 원본 타입 정의입니다.

### 2.1 공통 객체 렌더링 타입 (Rendered Object)
워드프레스 API의 `title`, `excerpt`, `content` 등은 단순 문자열이 아닌 `{ rendered: string }` 형태의 객체 구조를 갖습니다.

```typescript
/**
 * 워드프레스 HTML 렌더링 객체 타입
 */
export interface WPPostRendered {
  rendered: string;
  protected?: boolean;
}
```

---

### 2.2 썸네일 & 미디어 (WPMedia / WPFeaturedMedia)
`?_embed` 요청 시 `_embedded['wp:featuredmedia']` 항목으로 반환되는 이미지/미디어 타입입니다.

```typescript
/**
 * 워드프레스 이미지 사이즈 세부 정보
 */
export interface WPMediaSize {
  file: string;
  width: number;
  height: number;
  mime_type: string;
  source_url: string;
}

/**
 * 워드프레스 썸네일/미디어 객체
 */
export interface WPMedia {
  id: number;
  date: string;
  slug: string;
  type: string;
  link: string;
  title: WPPostRendered;
  author: number;
  alt_text: string;
  media_type: 'image' | 'file';
  mime_type: string;
  media_details: {
    width: number;
    height: number;
    file: string;
    sizes: {
      thumbnail?: WPMediaSize;
      medium?: WPMediaSize;
      medium_large?: WPMediaSize;
      large?: WPMediaSize;
      full?: WPMediaSize;
      [key: string]: WPMediaSize | undefined;
    };
  };
  source_url: string;
}
```

---

### 2.3 작성자 및 분류 용어 (WPAuthor & WPTerm)
`?_embed` 요청 시 포함되는 작성자(`author`) 및 카테고리/태그(`wp:term`) 타입입니다.

```typescript
/**
 * 워드프레스 작성자 객체
 */
export interface WPAuthor {
  id: number;
  name: string;
  url: string;
  description: string;
  link: string;
  slug: string;
  avatar_urls?: Record<string, string>;
}

/**
 * 워드프레스 분류(카테고리/태그) 용어 객체
 */
export interface WPTerm {
  id: number;
  link: string;
  name: string;
  slug: string;
  taxonomy: 'category' | 'post_tag' | string;
}
```

---

### 2.4 `_embed` 임베드 객체 (WPPostEmbedded)
`?_embed` 파라미터 요청 시 포스트 데이터 내부에 포함되어 썸네일, 작성자, 카테고리를 한번에 조회할 수 있는 타입입니다.

```typescript
/**
 * WP REST API ?_embed 요청 시 동동되는 임베드 데이터
 */
export interface WPPostEmbedded {
  'wp:featuredmedia'?: WPMedia[];
  'author'?: WPAuthor[];
  'wp:term'?: WPTerm[][]; // [0]: 카테고리 배열, [1]: 태그 배열
}
```

---

### 2.5 기사 포스트 모델 (WPPost)
워드프레스 `/wp-json/wp/v2/posts` 엔드포인트에서 반환되는 포스트 원본 타입입니다.

```typescript
/**
 * 워드프레스 포스트 (기사) 원본 인터페이스
 */
export interface WPPost {
  id: number;
  date: string; // ISO 8601 형식 (예: "2026-09-10T02:00:00")
  date_gmt: string;
  guid: WPPostRendered;
  modified: string;
  modified_gmt: string;
  slug: string;
  status: 'publish' | 'future' | 'draft' | 'pending' | 'private';
  type: string;
  link: string;
  title: WPPostRendered;
  content: WPPostRendered;
  excerpt: WPPostRendered;
  author: number;
  featured_media: number;
  comment_status: 'open' | 'closed';
  ping_status: 'open' | 'closed';
  sticky: boolean;
  template: string;
  format: string;
  categories: number[];
  tags: number[];
  _embedded?: WPPostEmbedded;
}
```

---

### 2.6 카테고리 모델 (WPCategory)
워드프레스 `/wp-json/wp/v2/categories` 엔드포인트에서 반환되는 카테고리 원본 타입입니다.

```typescript
/**
 * 워드프레스 카테고리 인터페이스
 */
export interface WPCategory {
  id: number;
  count: number;
  description: string;
  link: string;
  name: string;
  slug: string;
  taxonomy: 'category';
  parent: number;
  meta?: Record<string, unknown>;
}
```

---

## 3. 프론트엔드 전용 정규화 모델 & 유틸리티 (Normalized Frontend Model)

워드프레스의 복잡한 `{ rendered: string }` 및 `_embedded` 구조를 컴포넌트(React/Next.js)에서 사용하기 쉽도록 가공한 정규화(Normalized) 모델과 헬퍼 함수입니다.

### 3.1 정규화된 기사 타입 (NormalizedPost)

```typescript
/**
 * 프론트엔드 UI 컴포넌트에 최적화된 기사 데이터 타입
 */
export interface NormalizedPost {
  id: number;
  slug: string;
  title: string;
  excerpt: string;
  content: string;
  date: string; // ISO 원본 날짜
  formattedDate: string; // 예: "2026.09.10"
  imageUrl: string; // 썸네일 URL (없을 경우 fallback 이미지 URL)
  imageAlt: string; // 썸네일 대체 텍스트
  authorName: string;
  categoryName: string;
  categoryId: number | null;
  categorySlug: string;
}
```

---

### 3.2 정규화 헬퍼 함수 (normalizePost)

```typescript
const DEFAULT_FALLBACK_IMAGE = '/placeholder-news.png';

/**
 * HTML 태그 제거 유틸리티 (요약 텍스트 순수 텍스트화)
 */
function stripHtml(htmlString: string): string {
  return htmlString.replace(/<[^>]*>?/gm, '').trim();
}

/**
 * 날짜 포맷팅 유틸리티 (YYYY.MM.DD)
 */
function formatDate(dateString: string): string {
  const d = new Date(dateString);
  if (isNaN(d.getTime())) return dateString;
  const year = d.getFullYear();
  const month = String(d.getMonth() + 1).padStart(2, '0');
  const day = String(d.getDate()).padStart(2, '0');
  return `${year}.${month}.${day}`;
}

/**
 * WPPost 원본 응답을 UI 렌더링에 친화적인 NormalizedPost 구조로 정규화
 * 
 * @param post - 워드프레스 API 포스트 원본 객체
 * @param fallbackImage - 썸네일 미 존재 시 대체 이미지 경로
 */
export function normalizePost(
  post: WPPost,
  fallbackImage: string = DEFAULT_FALLBACK_IMAGE
): NormalizedPost {
  // 1. 대표 이미지 URL 및 Alt 텍스트 추출 (medium_large -> full -> source_url 순서)
  const featuredMedia = post._embedded?.['wp:featuredmedia']?.[0];
  const imageUrl =
    featuredMedia?.media_details?.sizes?.medium_large?.source_url ||
    featuredMedia?.source_url ||
    fallbackImage;
  const imageAlt = featuredMedia?.alt_text || post.title?.rendered || '기사 이미지';

  // 2. 작성자 이름 추출
  const authorObj = post._embedded?.['author']?.[0];
  const authorName = authorObj?.name || '하모니넷';

  // 3. 카테고리 이름 및 ID 추출 (첫 번째 카테고리 기준)
  const categoriesList = post._embedded?.['wp:term']?.[0] || [];
  const primaryCategory = categoriesList.find((term) => term.taxonomy === 'category');
  const categoryName = primaryCategory?.name || '뉴스';
  const categoryId = primaryCategory?.id || (post.categories?.[0] ?? null);
  const categorySlug = primaryCategory?.slug || 'news';

  return {
    id: post.id,
    slug: post.slug,
    title: post.title?.rendered || '',
    excerpt: stripHtml(post.excerpt?.rendered || ''),
    content: post.content?.rendered || '',
    date: post.date,
    formattedDate: formatDate(post.date),
    imageUrl,
    imageAlt,
    authorName,
    categoryName,
    categoryId,
    categorySlug,
  };
}
```

---

## 4. API 쿼리 파라미터 타입 (WP REST API Request Query Types)

Next.js 서버 컴포넌트(RSC) 및 API Client에서 사용할 요청 파라미터 타입 정의입니다.

```typescript
/**
 * 기사 목록 조회 요청 쿼리 파라미터
 */
export interface FetchPostsParams {
  page?: number;
  per_page?: number;
  search?: string;
  categories?: number | number[];
  slug?: string;
  _embed?: boolean; // 기본값 true 권장
  orderby?: 'date' | 'relevance' | 'id' | 'title' | 'slug';
  order?: 'asc' | 'desc';
}
```
