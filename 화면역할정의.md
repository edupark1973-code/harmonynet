# 하모니넷 (Harmonynet) 화면 역할 정의서 (화면역할정의.md)

---

## 1. 개요 (Overview)

본 문서는 **[시나리오.md](file:///C:/dev/harmonynet/시나리오.md)**, **[prd.md](file:///C:/dev/harmonynet/prd.md)**, **[데이터모델.md](file:///C:/dev/harmonynet/데이터모델.md)**를 바탕으로, Next.js (App Router) 환경에서 각 페이지 및 컴포넌트의 역할, Wireframe 구조, 데이터 흐름을 정의한 문서입니다.

---

## 2. 라우트 구조 및 파일 매핑 (Route Structure)

| 라우트 경로 | 담당 파일 | 페이지 구분 | SSR/Client 구분 | 주요 역할 |
| :--- | :--- | :--- | :--- | :--- |
| `/` | `app/layout.tsx` | 공통 레이아웃 | Server Component | Header(GNB), Footer, 전역 레이아웃 및 폰트 설정 |
| `/` | `app/page.tsx` | 메인 홈 페이지 | Server Component (ISR) | Hero 헤드라인 뉴스 + 포털 스타일 고밀도 카테고리 기사 리스트 |
| `/posts/[id]` | `app/posts/[id]/page.tsx` | 기사 상세 페이지 | Server Component (SSR/ISR) | 기사 제목, 본문 HTML 안전 렌더링, 사이드바 연관 기사 |
| `/search` | `app/search/page.tsx` | 검색 결과 페이지 | Client Component | 검색 키워드 기반 WP REST API 연동 결과 및 필터링 리스트 |
| `/category/[slug]` | `app/category/[slug]/page.tsx` | 카테고리별 기사 | Server Component (ISR) | 특정 카테고리 전용 기사 목록 페이지 |

---

## 3. 화면별 상세 구조 및 컴포넌트 역할

### 3.1 공통 레이아웃 (Layout / `app/layout.tsx`)

#### Wireframe 구조
```
+-------------------------------------------------------------------+
| [Header] 로고 | [GNB 카테고리: 지역소식 | 창업/스타트업 | 오피니언...] | [검색바] |
+-------------------------------------------------------------------+
|                                                                   |
|                       { children (페이지 본문) }                   |
|                                                                   |
+-------------------------------------------------------------------+
| [Footer] 발행인: 하모니넷 | 등록번호: 대전 아0000 | 대표전화 | Copyright... |
+-------------------------------------------------------------------+
```

#### 컴포넌트 역할 및 정의
* **`Header` (`components/layout/Header.tsx`)**:
  * **브랜드 로고**: 클릭 시 메인 페이지(`/`)로 이동.
  * **GNB (Global Navigation Bar)**: 카테고리 메뉴 목록(지역소식, 창업/스타트업, 오피니언, 문화/사회 등) 배치. 클릭 시 해당 카테고리(`/category/[slug]`) 이동.
  * **검색 바 (Search Bar)**: 검색 키워드 입력 후 Enter 또는 검색 버튼 클릭 시 `/search?q={query}`로 라우팅.
  * **모바일 햄버거 버튼**: 모바일 환경에서 슬라이딩 드로어 메뉴 표시.
* **`Footer` (`components/layout/Footer.tsx`)**:
  * 언론사 표준 정보 표기: 발행인/편집인 정보, 신문 등록번호, 주소 및 대표 연락처, 개인정보 처리방침 링크, Copyright 표시.

---

### 3.2 메인 페이지 (Home / `app/page.tsx`)

#### Wireframe 구조
```
+-------------------------------------------------------------------+
| [Hero Top News]                                                   |
| +-------------------------------+ +-------------------------------+ |
| | [큰 썸네일]                   | | [주요 헤드라인 2]             | |
| | 메인 헤드라인 기사 제목       | | 기사 제목 및 1~2줄 요약       | |
| +-------------------------------+ +-------------------------------+ |
+-------------------------------------------------------------------+
| [Latest News Grid - 한국형 네이버 뉴스 스탠드 스타일]             |
| +---------------------+ +---------------------+ +-----------------+ |
| | [카테고리: 창업]    | | [카테고리: 지역]    | | [오피니언]      | |
| | ㆍ 기사 제목 1 (New)| | ㆍ 기사 제목 1      | | ㆍ 칼럼 제목 1  | |
| | ㆍ 기사 제목 2      | | ㆍ 기사 제목 2      | | ㆍ 칼럼 제목 2  | |
| | ㆍ 기사 제목 3      | | ㆍ 기사 제목 3      | | ㆍ 칼럼 제목 3  | |
| +---------------------+ +---------------------+ +-----------------+ |
+-------------------------------------------------------------------+
```

#### 주요 구역 및 컴포넌트 역할
1. **Hero / Top News 구역 (`components/home/HeroNews.tsx`)**:
   * 가장 중요한 메인 헤드라인 기사 1~2개를 큰 썸네일과 볼드체 제목으로 노출하여 시선 집중.
2. **Latest News List 구역 (`components/home/NewsGrid.tsx`)**:
   * 네이버 뉴스 스탠드 스타일의 **고밀도 3열 그리드 / 콤팩트 리스트** 배치.
   * 카테고리별로 상위 3~5개 기사 제목과 미니 썸네일/발행일 표시.
3. **썸네일 미존재 시 Fallback UI 처리**:
   * `NormalizedPost.imageUrl`이 지정되지 않았거나 워드프레스 이미지 불러오기 실패 시 `next/image` onError 핸들러 및 Fallback 로직 적용.
   * 하모니넷 브랜딩 로고가 포함된 플레이스홀더 이미지(`public/placeholder-news.png`) 표시.

---

### 3.3 기사 상세 페이지 (Article Detail / `app/posts/[id]/page.tsx`)

#### Wireframe 구조
```
+---------------------------------------------------+ +-------------+
| [Category Badge] 창업/스타트업                      | | [Sidebar]   |
| [Title] 대전·충청 스타트업 지원 사업 2026 본격화...  | |             |
| [Meta] 작성자: 김기자 | 입력: 2026.09.10 02:00      | | 인기 기사   |
+---------------------------------------------------+ | 1. 기사제목 |
| [Featured Image] 기사 대표 썸네일 이미지          | | 2. 기사제목 |
+---------------------------------------------------+ |             |
| [Article Content]                                 | | 연관 기사   |
| Tailwind Typography (`prose`) 적용 본문 HTML       | | 1. 기사제목 |
| (dangerouslySetInnerHTML 안전 렌더링)             | | 2. 기사제목 |
+---------------------------------------------------+ +-------------+
| [Navigation] ◀ 이전 기사 | 다음 기사 ▶            |
+---------------------------------------------------+
```

#### 컴포넌트 역할 및 안전한 본문 렌더링
* **`ArticleHeader` (`components/post/ArticleHeader.tsx`)**:
  * 카테고리 태그, 기사 제목, 작성자, 작성일시 표시.
* **`ArticleBody` (`components/post/ArticleBody.tsx`)**:
  * 워드프레스 API의 `post.content.rendered` HTML을 `dangerouslySetInnerHTML`로 렌더링.
  * `@tailwindcss/typography`의 `prose prose-lg dark:prose-invert` 클래스를 적용하여 본문 폰트, 줄간격, 인용구, 이미지 자동 스타일링.
* **`ArticleSidebar` (`components/post/ArticleSidebar.tsx`)**:
  * 동일 카테고리 연관 기사 목록 및 조회수 기반/최신 인기 기사 탑 5 노출.
* **`ArticleNav` (`components/post/ArticleNav.tsx`)**:
  * 이전 기사/다음 기사 텍스트 링크 버튼 제공.

---

### 3.4 검색 결과 페이지 (Search / `app/search/page.tsx`)

#### Wireframe 구조
```
+-------------------------------------------------------------------+
| [Search Header] "'대전 창업'" 검색 결과 (총 12건)                  |
| [Search Input Bar] [ 대전 창업                      ] [ 검색 ]   |
+-------------------------------------------------------------------+
| [Search Result List]                                              |
| +---------------------------------------------------------------+ |
| | [미니 썸네일] | [제목] 2026년 대전 창업 지원 사업 가이드      | |
| |               | [요약] 대전시가 스타트업 생태계를 활성화하기...  | |
| |               | [Meta] 창업/스타트업 | 2026.09.10             | |
| +---------------------------------------------------------------+ |
| +---------------------------------------------------------------+ |
| | [미니 썸네일] | [제목] 대전 스타트업 투자 유치 성공 소식       | |
| +---------------------------------------------------------------+ |
| [Pagination / Load More Button]                                   |
+-------------------------------------------------------------------+
```

#### 컴포넌트 역할 및 데이터 처리
* **`SearchHeader` (`components/search/SearchHeader.tsx`)**:
  * 현재 입력된 검색 키워드 및 검색 결과 개수 안내.
* **`SearchResultList` (`components/search/SearchResultList.tsx`)**:
  * Client Component로 구동되며, URL 쿼리스트링(`?q=keyword`) 변경 감지 시 워드프레스 REST API (`/posts?search={q}&_embed`) 연동.
  * 검색된 기사 리스트를 고밀도 텍스트 리스트 형태로 출력.
* **`EmptyState` (`components/search/EmptyState.tsx`)**:
  * 검색 결과가 없을 경우 "검색 결과가 없습니다. 다른 검색어를 입력해 보세요." 안내 메시지 출력.

---

## 4. 컴포넌트 데이터 인터페이스 매핑

[데이터모델.md](file:///C:/dev/harmonynet/데이터모델.md)의 `NormalizedPost` 모델을 주요 컴포넌트의 Props로 적극 활용합니다.

```typescript
// components/common/NewsCard.tsx 예시
import { NormalizedPost } from '@/types/post';

interface NewsCardProps {
  post: NormalizedPost;
  variant?: 'hero' | 'compact' | 'list';
}

export function NewsCard({ post, variant = 'compact' }: NewsCardProps) {
  // variant별 렌더링 지원 (Hero / 3열 그리드 / 리스트형)
}
```
