# 하모니넷 (Harmonynet) 기술 아키텍처 및 개발 명세서 (테크.md)

---

## 1. 개요 (Overview)

본 문서는 **[시나리오.md](file:///C:/dev/harmonynet/시나리오.md)**, **[prd.md](file:///C:/dev/harmonynet/prd.md)**, **[데이터모델.md](file:///C:/dev/harmonynet/데이터모델.md)**, **[화면역할정의.md](file:///C:/dev/harmonynet/화면역할정의.md)**를 기반으로, 개발자가 코딩 및 배포 과정에서 즉시 참고할 수 있는 기술 아키텍처, 디렉토리 구조, 데이터 페칭 전략 및 모듈 설계를 명시합니다.

---

## 2. 기술 스택 (Tech Stack)

| 구분 | 기술 / 도구 | 선정 사유 및 세부 설정 |
| :--- | :--- | :--- |
| **Framework** | **Next.js (App Router)** | Server Components, SSR/ISR 최적화, 최신 App Router 아키텍처 적용 |
| **Language** | **TypeScript 5.x** | 정적 타입을 통한 개발 안정성 및 데이터 모델 연동 신뢰성 확보 |
| **Styling** | **Tailwind CSS v4** | 고밀도 한국형 뉴스 스탠드 레이아웃 및 `@tailwindcss/typography` 본문 스타일링 |
| **Data Fetching** | **Native Fetch API** | 공개 WP 콘텐츠는 브라우저 직접 조회, 서버 프록시는 진단·향후 WAF 정책 변경 대비 용도로 유지 |
| **Hosting & Infra** | **Firebase App Hosting** | Next.js SSR 및 정적 자원 전용 파이어베이스 자동 배포 (리전: `asia-east1` 대만) |
| **CMS (Backend)** | **Cafe24 WordPress REST API** | 기존 기사 데이터베이스 및 기자단 에디터 유지 (`https://huss.harmonynet.kr/wp-json/wp/v2/`) |

---

## 3. 프로젝트 디렉토리 구조 (Directory Structure)

```
harmonynet/
├── app/                        # Next.js App Router 라우트 영역
│   ├── category/               # 카테고리 기사 리스트
│   │   └── [slug]/
│   │       └── page.tsx
│   ├── posts/                  # 기사 상세 페이지
│   │   └── [id]/
│   │       └── page.tsx
│   ├── search/                 # 검색 결과 페이지
│   │   └── page.tsx
│   ├── error.tsx               # 전역 에러 핸들러
│   ├── layout.tsx              # Root Layout (GNB Header, Footer)
│   ├── loading.tsx             # 전역 스켈레톤 로딩 UI
│   ├── not-found.tsx           # 404 페이지
│   └── page.tsx                # 메인 홈 페이지 (Hero + Latest Grid)
├── components/                 # React UI 컴포넌트
│   ├── common/                 # 공통 컴포넌트 (NewsCard, Pagination 등)
│   │   ├── NewsCard.tsx
│   │   └── Skeleton.tsx
│   ├── home/                   # 메인 홈 전용 컴포넌트
│   │   ├── HeroNews.tsx
│   │   └── NewsGrid.tsx
│   ├── layout/                 # 공통 레이아웃 컴포넌트
│   │   ├── Footer.tsx
│   │   ├── Header.tsx
│   │   └── Navigation.tsx
│   ├── post/                   # 기사 상세 전용 컴포넌트
│   │   ├── ArticleBody.tsx
│   │   ├── ArticleHeader.tsx
│   │   ├── ArticleNav.tsx
│   │   └── ArticleSidebar.tsx
│   └── search/                 # 검색 전용 컴포넌트
│       ├── EmptyState.tsx
│       └── SearchHeader.tsx
├── lib/                        # 비즈니스 로직, API 연동, 유틸리티
│   ├── api/
│   │   └── wp.ts               # WP REST API 전담 Fetcher 모듈
│   └── utils.ts                # 데이터 정규화(normalizePost), 날짜, HTML 처리
├── types/                      # TypeScript 타입 정의
│   ├── post.ts                 # WPPost, NormalizedPost 타입 (데이터모델.md 연동)
│   └── category.ts             # WPCategory 타입
├── public/                     # 정적 자원 (로고, 플레이스홀더 이미지)
│   ├── logo.svg
│   ├── next.svg
│   └── placeholder-news.png    # 썸네일 부재 시 Fallback 이미지
├── apphosting.yaml             # Firebase App Hosting 설정 파일
├── next.config.ts              # Next.js 설정 (remotePatterns 등)
├── package.json
└── tsconfig.json
```

---

## 4. 데이터 페칭 전략 (Data Fetching Strategy)

### 4.0 2026-09-10 운영 결정

Firebase App Hosting 서버에서 Cafe24 WordPress REST API를 호출할 때 WAF가 `200 text/html` 차단 페이지를 반환해 프록시가 500/502로 실패한다. 따라서 현재 메인, 카테고리, 검색, 기사 상세, 섹션 페이지는 Client Component에서 `https://huss.harmonynet.kr/wp-json/wp/v2/`를 직접 조회한다.

기사 상세는 단건 `/posts/{id}?_embed` 대신 WAF 호환성이 확인된 `/posts?_embed=1&include={id}&per_page=1` 형식을 사용한다. 서버 프록시를 다시 활성화하려면 Cafe24에서 Firebase 송신 요청을 허용한 뒤 실제 배포 환경에서 JSON Content-Type을 검증해야 한다.

현행 추가 구조는 다음과 같다.

* `components/SiteNav.tsx`: 데스크톱·모바일 공통 메뉴
* `app/section/[slug]/page.tsx`: 영상뉴스 및 콘텐츠기업소개
* `app/api/posts/route.ts`, `app/api/pages/route.ts`: WAF 정책 변경에 대비해 유지하는 서버 프록시

### 4.1 WP REST API Fetcher 및 정규화 모듈 (`lib/wp.ts`)

Next.js `fetch`의 캐시 옵션을 활용하여 **서버 사이드 렌더링(SSR)**과 **증분 정적 재생성(ISR)**을 효율적으로 구현합니다.

```typescript
import { WPPost, WPCategory } from '@/types/post';
import { normalizePost, NormalizedPost } from '@/lib/utils';

const WP_API_BASE = process.env.NEXT_PUBLIC_WP_API_URL || 'https://huss.harmonynet.kr/wp-json/wp/v2';

/**
 * 기본 WP Fetcher 공통 유틸리티 (에러 핸들링 & 타임아웃 포함)
 */
async function fetchWP<T>(endpoint: string, options: RequestInit = {}): Promise<T> {
  const url = `${WP_API_BASE}${endpoint}`;

  try {
    const res = await fetch(url, {
      headers: {
        'Content-Type': 'application/json',
      },
      ...options,
    });

    if (!res.ok) {
      throw new Error(`[WP API Error] ${res.status} ${res.statusText} on ${endpoint}`);
    }

    return await res.json();
  } catch (error) {
    console.error(`[Fetch Failure] ${url}`, error);
    throw error;
  }
}

/**
 * 최신 기사 목록 조회 (ISR 60초 캐싱 적용)
 */
export async function getPosts(params: {
  page?: number;
  perPage?: number;
  category?: number;
} = {}): Promise<NormalizedPost[]> {
  const { page = 1, perPage = 10, category } = params;
  let endpoint = `/posts?_embed&page=${page}&per_page=${perPage}`;
  if (category) endpoint += `&categories=${category}`;

  const posts = await fetchWP<WPPost[]>(endpoint, {
    next: { revalidate: 60, tags: ['posts'] }, // 60초 주기 ISR
  });

  return posts.map((post) => normalizePost(post));
}

/**
 * 단일 기사 상세 조회 (ID 기반)
 */
export async function getPostById(id: string | number): Promise<NormalizedPost | null> {
  try {
    const post = await fetchWP<WPPost>(`/posts/${id}?_embed`, {
      next: { revalidate: 300, tags: [`post-${id}`] },
    });
    return normalizePost(post);
  } catch (error) {
    return null;
  }
}

/**
 * 키워드 기사 검색
 */
export async function searchPosts(query: string, page = 1): Promise<NormalizedPost[]> {
  if (!query.trim()) return [];
  const endpoint = `/posts?_embed&search=${encodeURIComponent(query)}&page=${page}&per_page=10`;
  const posts = await fetchWP<WPPost[]>(endpoint, {
    cache: 'no-store', // 검색은 실시간 결과 반환
  });
  return posts.map((post) => normalizePost(post));
}
```

---

## 5. HTML 본문 안전 렌더링 & 스타일링 (HTML Body Rendering)

워드프레스 에디터에서 전송되는 `content.rendered` HTML 문자열을 안전하고 가독성 있게 렌더링하기 위한 방안입니다.

### 5.1 안전한 HTML 렌더링 컴포넌트 (`components/post/ArticleBody.tsx`)

```tsx
import React from 'react';

interface ArticleBodyProps {
  contentHtml: string;
}

export function ArticleBody({ contentHtml }: ArticleBodyProps) {
  // 실제 프로덕션 환경에서는 DOMPurify 또는 sanitize-html 라이브러리로 XSS 방지 권장
  return (
    <div
      className="prose prose-zinc max-w-none dark:prose-invert prose-headings:font-bold prose-a:text-blue-600 prose-img:rounded-lg prose-img:shadow-md"
      dangerouslySetInnerHTML={{ __html: contentHtml }}
    />
  );
}
```

### 5.2 `next.config.ts` 외부 이미지 도메인 허용 설정

워드프레스 카페24 서버 및 `harmonynet.kr` 호스트 이미지가 `next/image` 컴포넌트에서 거부되지 않도록 등록합니다.

```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'harmonynet.kr',
      },
      {
        protocol: 'http',
        hostname: 'harmonynet.kr',
      },
      {
        protocol: 'https',
        hostname: '*.cafe24.com',
      },
    ],
  },
};

export default nextConfig;
```

---

## 6. 배포 및 인프라 설정 (Firebase App Hosting)

Firebase App Hosting을 통해 Next.js App Router 서버 사이드 렌더링(SSR) 애플리케이션을 대만 리전(`asia-east1`)에 배포합니다.

### `apphosting.yaml` 설정 파일
```yaml
kind: AppHostingYaml
schemaVersion: v1
env:
  - variable: NEXT_PUBLIC_WP_API_URL
    value: "https://huss.harmonynet.kr/wp-json/wp/v2"
runConfig:
  region: asia-east1
  concurrency: 80
  cpu: 1
  memoryMiB: 512
```
